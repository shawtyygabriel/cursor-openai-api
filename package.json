{
  "name": "cursor-openai-api",
  "version": "0.0.3",
  "description": "Standalone CLI that serves Cursor API as OpenAI-compatible",
  "type": "module",
  "bin": "./dist/cli.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-runtime.mjs",
    "dev": "bun run src/cli.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^5.9.3"
  }
}
